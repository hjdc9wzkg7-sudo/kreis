# Expo

This project targets Expo SDK 54, the version currently shipped by Expo Go on the iOS App Store.

Read the versioned docs at https://docs.expo.dev/versions/v54.0.0/ before changing native or Expo packages.
